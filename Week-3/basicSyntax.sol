// SPDX-License-Identifier: MIT
pragma solidity ^0.8.17;

contract basicSyntax{
    // Example delcarations
    uint256 number = 42;
    string message = "Hello, world!";
    bool isTrue = true;

    // Example operations
    uint256 a = 10;
    uint256 b = 5;
    uint256 c = a + b; // Addition
    uint256 d = a * b; // 
    
    // Declare a variable called balance and assign a value
    uint balance = 100;

    // Declare another variable called price and assign a value
    uint price = 10;

    // Calculate the new balance after purchasing an item with the price and store in a another variable
    uint newBalance = balance - price;
}